# Commands
* G -> generate
* Space -> Skip animation 
* S -> Solve
* C -> Change color
* R -> Re-generate
* P -> Show/Hide path
* M -> Game Mode

# Bonus
## in the game mode:
 * S -> Start
 * A -> Skill: ariadne path (Only after the Minotaur diesz)